# Limitless API credentials
LIMITLESS_API_KEY=your_api_key_here

# Database configuration
DB_TYPE=sqlite
DB_PATH=./data/limitless.db

# Server configuration
PORT=3000
ENABLE_SERVER=true

# Cron schedule for data ingestion (every 30 minutes by default)
INGESTION_SCHEDULE=*/30 * * * *
